# Tools

Tools let a model access capabilities beyond text generation, such as search, calculation, file access, or controlled actions.

When a model calls a tool, the application executes that capability and returns the result so the model can continue.
