# Tools

Tools are capabilities that a model can call when text generation alone is not enough.

Examples include search, calculation, file access, and structured transformations. Tools matter because they let the model work with information and actions outside its built-in context.
