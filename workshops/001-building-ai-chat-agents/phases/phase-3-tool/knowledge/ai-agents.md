# AI Agents

An AI agent is a system that combines a language model with a loop and one or more capabilities outside the model itself.

The useful mental model is:

`model + tools + loop`

Plain chat usually means prompt in, answer out. An agent can inspect state, call a tool, observe the result, and then continue before answering.
