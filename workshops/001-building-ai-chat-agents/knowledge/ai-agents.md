# AI Agents

An AI agent is a system where a language model can reason over the current task, use capabilities outside itself, and continue the loop before answering.

The minimal mental model is:

`model + tools + loop`

Memory can support the loop, but tools are what let the system do more than plain chat.
